DATABASE_URL=file:/home/z/my-project/db/custom.db

# JWT secret for auth sessions (change in production!)
JWT_SECRET=shadowvault-super-secret-jwt-key-2025-change-me

# Razorpay test mode keys (public test keys — safe to commit for demo)
# Replace with real keys in production via environment variables.
RAZORPAY_KEY_ID=rzp_test_1BAPLkErYJQxLE
RAZORPAY_KEY_SECRET=HkX0JjQ2kR3mS4tUvWxYzA1bC2dE3fG4

# ============================================================================
# FIRESTORE (Firebase) — Database configuration
# ============================================================================
# The app now uses Firestore instead of SQLite/Prisma.
#
# To get these values:
# 1. Go to https://console.firebase.google.com/ and create a project.
# 2. Project Settings → Service Accounts → Generate new private key (JSON).
# 3. Copy the values from that JSON file into the env vars below.
#
#   FIREBASE_PROJECT_ID   →  "project_id" in the JSON
#   FIREBASE_CLIENT_EMAIL →  "client_email" in the JSON
#   FIREBASE_PRIVATE_KEY  →  "private_key" in the JSON (keep the quotes,
#                            it contains \n line breaks)
FIREBASE_PROJECT_ID=shadowvault-demo
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@shadowvault-demo.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nREPLACE_WITH_YOUR_KEY\n-----END PRIVATE KEY-----\n"
